# Legacy Root Tests

These files were previously scattered in the project root.

They are preserved here for manual or legacy verification, while the actively maintained test suite remains under `tests/` and `tests/forecasting/`.
