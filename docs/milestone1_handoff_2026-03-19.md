# 里程碑 1 接手说明

日期：2026-03-19

## 当前结论

第一个里程碑的代码骨架已经完成，且关键链路已通过自动化测试：

- 新统一入口：`src/api/app.py`
- 新任务平台存储：`src/api/platform_store.py`
- 新任务运行层：`src/api/runtime.py`
- 新前端控制台：`frontend/index.html`
- 新样式与交互：`static/platform.css`、`static/platform.js`
- 结果查询与 `run_id/config_id` 入库支持：`src/database/repositories.py`

## 已验证通过

执行命令：

```powershell
pytest tests\test_forecast_platform.py -q
```

结果：

- `9 passed`

覆盖内容：

- 手工 SPU 清单解析
- 平台配置存储 roundtrip
- FastAPI 路由注册
- 新前端入口返回新版控制台 HTML
- `manual` 方式 SPU 解析接口
- 配置模板创建接口
- 执行计划创建接口
- 任务创建接口（stub manager 验证）
- 兼容状态与日志接口
- 结果查询接口（stub manager 验证）

## 已实现的里程碑 1 能力

- 三种 SPU 选择模式的接口协议已经建立：
  - `all`
  - `manual`
  - `sql`
- 新前端已保留并重组核心运行信息区域：
  - 整体进度
  - SPU 状态
  - 执行日志
- 已支持：
  - 创建任务
  - 停止任务
  - 查询任务
  - 查询日志
  - 查询 SPU 状态
  - 保存配置模板
  - 保存周计划
  - 查询结果

## 当前未完全打透的点

- 本机浏览器实时联调存在环境级阻塞：
  - `127.0.0.1:8000` 曾被旧版 V2 页面进程占用
  - 新版服务前台启动正常，但在当前 Codex/Playwright 工具环境下，本地端口联调存在会话隔离现象
- 因此目前“代码 + API + 自动化验收”已通过，“桌面手工点点点验收”仍建议在用户本机真实终端中再跑一轮

## 下一步建议

1. 清理本机历史 `8000` 端口服务，只保留新版 `uvicorn src.api.app:app`
2. 用真实数据库连通性跑一轮手工冒烟：
   - 全选预览
   - 粘贴 SPU 预览
   - 保存配置
   - 保存周计划
   - 启动任务
   - 查看进度 / SPU 状态 / 日志
3. 如果真实任务运行正常，再进入里程碑 2：
   - 历史运行查询
   - 结果页增强
   - 配置编辑与计划开关

## 备注

- 当前实现优先保证“平台化结构”与“里程碑 1 可扩展性”
- 前端是 build-free 单页控制台，不依赖前端构建链，便于先验证能力
- 后续若决定正式切 React 工程，可直接复用现有 API，不需要重做后端
